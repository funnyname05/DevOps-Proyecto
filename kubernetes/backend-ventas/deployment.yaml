apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-ventas
  namespace: proyecto
spec:
  replicas: 2
  selector:
    matchLabels:
      app: back-ventas
  template:
    metadata:
      labels:
        app: back-ventas
    spec:
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: back-ventas
        image: 895716090405.dkr.ecr.us-east-1.amazonaws.com/devops/back-ventas:v1
        ports:
        - containerPort: 8080
        env:
        - name: DB_ENDPOINT
          value: "proyecto-db-service"
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "db_ventas"
        - name: DB_USERNAME
          value: "user_ventas"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: DB_PASSWORD_VENTAS
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
