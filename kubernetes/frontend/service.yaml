apiVersion: v1
kind: Service
metadata:
  name: front-despacho-service
  namespace: proyecto
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  selector:
    app: front-despacho
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
